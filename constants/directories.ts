import path from 'path';

export const POST_DIRECTORY = path.join(process.cwd(), 'contents', 'posts');
export const CONTACT_DIRECTORY = path.join(
  process.cwd(),
  'contents',
  'contact',
);
export const CONTACT_FILE_NAME = 'contact';
